<div class="icon-list">
    <ul>
        {% for tag in product.tags %}
            {% if tag contains 'momis_pick' %}
                <li>
                    <img src="{{ 'momi_48px.svg' | asset_url }}" alt="Momi's Pick">
            {% break %}
                </li>
            {% endif %}
        {% endfor %}
        {% for tag in product.tags %}
            {% if tag contains 'clean_beauty' %}
                <li>
                    <img src="{{ 'clean_48px.svg' | asset_url }}" alt="Clean Beauty">
            {% break %}
                </li>
            {% endif %}
        {% endfor %}
        {% for tag in product.tags %}
            {% if tag contains 'freeshipping_4life' %}
                <li>
                    <img src="{{ 'freeshipping_4life_48px.svg' | asset_url }}" alt="Free Shipping 4 life">
            {% break %}
                </li>
            {% endif %}
        {% endfor %}
    </ul>
</div>