{%- comment -%}
Breadcrumbs snippet, le falta agregar el container div con su class correspondiente.
{%- endcomment -%}
{%- unless template == 'index' or template == 'cart' or template == 'list-collections' or template == '404' -%}
{%- assign t = template | split: '.' | first -%}
    <p>
        <a href="{{ blog.url }}">{{ blog.title }}</a>
    {%- case t -%}
        {%- when 'blog' -%}
            &nbsp;/ <a href="{{ blog.url }}" aria-current="page">{{- blog.title -}}</a>
        {%- when 'article' -%}
            &nbsp;/ {% for tag in article.tags %}
            <a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag }}{% unless forloop.last %}, {% endunless %}</a>
            {% endfor %}
            
    {%- endcase -%}
    </p>
{%- endunless -%}
{%- comment -%}
Breadcrumbs snippet END
{%- endcomment -%}